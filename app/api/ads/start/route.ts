import { NextRequest, NextResponse } from "next/server";
import crypto from "crypto";
import { createClient } from "@supabase/supabase-js";
const SUPABASE_URL="https://glkpxyanjsktmwkvvsxt.supabase.co",MONETAG_ZONE_ID="11801942",SESSION_TTL_MS=15*60*1000;
const BOT_TOKEN=()=> (process.env.TELEGRAM_BOT_TOKEN||"").trim();
function getTelegramUser(initData:string){const botToken=BOT_TOKEN();if(!botToken||!initData)return null;const params=new URLSearchParams(initData),hash=params.get("hash");if(!hash)return null;params.delete("hash");const check=[...params.entries()].sort(([a],[b])=>a.localeCompare(b)).map(([k,v])=>`${k}=${v}`).join("\n");const secret=crypto.createHmac("sha256","WebAppData").update(botToken).digest();const calculated=crypto.createHmac("sha256",secret).update(check).digest("hex");if(hash.length!==calculated.length||!crypto.timingSafeEqual(Buffer.from(hash),Buffer.from(calculated)))return null;const raw=params.get("user");if(!raw)return null;try{return JSON.parse(raw) as {id?:number}}catch{return null}}
export async function POST(req:NextRequest){try{const body=await req.json().catch(()=>null),tg=getTelegramUser(String(body?.initData||"")),serviceKey=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!tg?.id)return NextResponse.json({error:"INVALID_TELEGRAM_SESSION"},{status:401});if(!serviceKey)return NextResponse.json({error:"SERVER_CONFIG_ERROR"},{status:500});const db=createClient(SUPABASE_URL,serviceKey,{auth:{autoRefreshToken:false,persistSession:false}});const {data:user,error:userError}=await db.from("users").select("id,activated,status,account_level").eq("telegram_id",tg.id).maybeSingle();if(userError)return NextResponse.json({error:"TEMPORARY_CONNECTION_PROBLEM"},{status:503});if(!user)return NextResponse.json({error:"USER_NOT_FOUND"},{status:404});if(user.status!=="active")return NextResponse.json({error:"ACCOUNT_NOT_ACTIVE"},{status:403});if(!user.activated)return NextResponse.json({error:"ACCOUNT_ACTIVATION_REQUIRED"},{status:403});const {data:plan,error:planError}=await db.from("activation_levels").select("daily_ad_limit,active").eq("level",user.account_level).maybeSingle();if(planError)return NextResponse.json({error:"DATABASE_ERROR"},{status:500});if(!plan||!plan.active)return NextResponse.json({error:"PLAN_NOT_AVAILABLE"},{status:403});const start=new Date();start.setHours(0,0,0,0);const {count}=await db.from("ad_rewards").select("id",{count:"exact",head:true}).eq("user_id",user.id).gte("created_at",start.toISOString());const limit=Number(plan.daily_ad_limit||0);if(limit>0&&(count||0)>=limit)return NextResponse.json({error:"DAILY_AD_LIMIT_REACHED",message:`Your plan allows ${limit} ads per day.`},{status:429});const now=Date.now();await db.from("ad_sessions").update({status:"expired",completed_at:new Date(now).toISOString()}).eq("user_id",user.id).eq("provider","monetag").eq("status","started").lt("started_at",new Date(now-SESSION_TTL_MS).toISOString());const requestVar=`watch_ads_${now}_${crypto.randomBytes(12).toString("hex")}`,ymid=`vc_${crypto.randomBytes(16).toString("hex")}`;const {data:inserted,error}=await db.from("ad_sessions").insert({user_id:user.id,provider:"monetag",zone_id:MONETAG_ZONE_ID,request_var:requestVar,ymid,status:"started"}).select("id").single();if(error||!inserted)return NextResponse.json({error:"AD_SESSION_ERROR"},{status:500});return NextResponse.json({ok:true,session_id:inserted.id,request_var:requestVar,ymid,zone_id:MONETAG_ZONE_ID});}catch(error){console.error("ViewCash ad start error",error);return NextResponse.json({error:"AD_SESSION_ERROR"},{status:500})}}
